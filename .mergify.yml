queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions: []
    branch_protection_injection_mode: queue
    merge_method: squash
pull_request_rules:
  - name: controlled autoqueue ruleset validation
    conditions:
      - title = H1 controlled ruleset exemption test
    actions:
      queue:
